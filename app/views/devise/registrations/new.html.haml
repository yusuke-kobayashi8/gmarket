.signup-container
  = render 'signup-header'

  %main.signup-main
    .signup-main__box
      %h2.signup-main__box__top 会員情報入力
      .signup-main__contents
      = form_for(@user, url: user_registration_path) do |f|
        -# = render "devise/shared/error_messages", resource: @user
        .field
          = f.label :ニックネーム
          %span.form-require 必須
          %br/
          = f.text_field :nickname, autofocus: true, autocomplete: "nickname", placeholder: "例）フリマ太郎"
        .field
          = f.label :Eメール
          %span.form-require 必須
          %br/
          = f.email_field :email, autocomplete: "email", placeholder: "PC・携帯どちらでも可"
        .field
          = f.label :パスワード
          %span.form-require 必須
          - if @minimum_password_length
            -# %em
            -#   (#{@minimum_password_length}文字以上)
          %br/
          = f.password_field :password, autocomplete: "new-password", placeholder: "７文字以上の半角英数字"
        .field
          = f.label :確認用パスワード
          %span.form-require 必須
          %br/
          = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "確認用パスワード"
        .text-box
          %h3.text-left 本人確認
          %p 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
        = f.label :お名前（全角）
        %span.form-require 必須
        %br/
        .fullname
          .field
            = f.text_field :family_name, autocomplete: "family_name", placeholder: "例）山田"
          .field       
            = f.text_field :first_name, autocomplete: "first_name", placeholder: "例）太郎"    
        = f.label :お名前カナ（全角）
        %span.form-require 必須
        %br/
        .fullname-kana  
          .field
            = f.text_field :family_name_kana, autocomplete: "family_name_kana", placeholder: "例）ヤマダ"
          .field      
            = f.text_field :first_name_kana, autocomplete: "first_name_kana", placeholder: "例）タロウ"  
        .field
          = f.label :生年月日
          %span.form-require 必須
          %br/
          = f.date_select :birthday, use_month_numbers: true,start_year: 1930, end_year: (Time.now.year - 10), default: Date.new(1989, 1, 1)
        %p.form-info-text ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
        %p.form-text
          「次へ進む」のボタンを押すことにより、
          = link_to("#") do
            利用規約
          に同意したものとみなします
        .actions
          = f.submit "次へ進む", class: 'submit-btn'
        %p.form-text-right
          = link_to("#") do
            本人情報の登録について
            %i.fas.fa-angle-right
        %p.form-bottom-text This site is protected by reCAPTCHA and the Google Privacy Policy and Terms of Service apply.
          

  .footer
    =render '/products/footer'